[project]
name = "hardn"
version = "0.1.0"
description = "A project to orchestrate system setup and kernel hardening using C and Bash"
authors = [
    { name = "SIG team", email = "support@SIG.org" }
]
license = { text = "GNU" }
readme = "README.md"
keywords = ["security", "orchestration", "c", "bash", "kernel"]
classifiers = [
    "Programming Language :: C",
    "Programming Language :: Unix Shell",
    "License :: OSI Approved :: GNU License",
    "Operating System :: POSIX :: Debian",
    "Topic :: System :: Systems Security",
    "Topic :: Security"
]

[build-system]
requires = []
build-backend = "setuptools.build_meta"